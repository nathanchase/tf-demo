# TMDb API key (v3 auth) — used server-side only for poster/artwork enrichment.
# Get one free at https://www.themoviedb.org/settings/api
# The app still works without it (text-only); recommendations are unaffected.
TMDB_API_KEY=
