# TMDb API key (v3 auth) — only needed to RE-BAKE the catalog artwork via
# `pnpm generate:catalog`. The app itself ships with the enriched catalog
# committed (server/utils/enriched-catalog.json) and needs no key to run.
# Get one free at https://www.themoviedb.org/settings/api
TMDB_API_KEY=
